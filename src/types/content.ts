export interface ContentMeta {
  title: string;
  area: string;     // ex: "infra"
  subarea: string;  // ex: "redes"
  category: string; // ex: "fundamentos"
  prerequisites: string[];
  next_topics: string[];
  tags: string[];
}
